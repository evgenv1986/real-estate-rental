[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = find:
package-dir = {"": "src"}

[project]
dependencies = [
    "common @ file:///Users/evgeny/Documents/Projects/real_estate_rental/common"
]
